"""Local resume-tailoring agent.

Rewrites only the tailorable sections of a Markdown resume to better match a
job description, while preserving the document's identity: protected sections,
dates, numbers, company names, and overall length stay intact.
"""
